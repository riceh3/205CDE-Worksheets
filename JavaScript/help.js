var tasks = []; // empty array

function addToArray(){                // function to push to array
    'use strict';

    console.log('Testing');

    var add = (3);

    tasks.push(add);

    console.log(tasks);
}
window.onload = addToArray;
